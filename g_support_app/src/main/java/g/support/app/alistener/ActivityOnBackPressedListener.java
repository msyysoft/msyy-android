package g.support.app.alistener;

import androidx.appcompat.app.AppCompatActivity;

public interface ActivityOnBackPressedListener {
    boolean whenActivityOnBackPressed(AppCompatActivity activity);
}